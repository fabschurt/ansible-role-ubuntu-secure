---
- name: Update GRUB
  shell: 'update-grub2 || update-grub'

- name: Restart SSH
  service:
    name: ssh
    state: restarted

- name: Restart networking
  shell: 'ifdown --exclude=lo -a && sleep 2 && ifup --exclude=lo -a'

- name: Reboot server
  shell: 'sleep 2 && reboot'
  async: 1
  poll: 0
  ignore_errors: true
